import { ArrowLeft, IconProps } from '@phosphor-icons/react'

export type ArrowLeftIconProps = IconProps

export function ArrowLeftIcon(props: IconProps) {
  return <ArrowLeft size={32} {...props} />
}
